## Removing Branches Local

```
      $  git branch -D dev 
      $  git branch -D test
```
## Removing Branches REMOTE
After Deleting
```
        $ git push origin : dev 
        $ git push origin : test 
```
